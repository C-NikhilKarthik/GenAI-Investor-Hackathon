

import { RequestHandler, Response } from 'express';

import { IRequest } from '../types/IRequest';


export const convertAudioToText:RequestHandler=async(req:IRequest,res:Response,next)=>{
        try{

        }
        catch{

        }
};

